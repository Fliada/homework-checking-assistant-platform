# Скользящее среднее

Реализуйте moving_average(values, window): средние только полных окон; положительное целое window, иначе ValueError. Алгоритм O(n), вход не меняется, если элементов меньше окна — [].

Python 3.11, стандартная библиотека.
