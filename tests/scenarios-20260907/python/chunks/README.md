# Пакетная обработка

Реализуйте chunks(iterable, size): ленивый генератор списков длиной size; последний может быть короче. Вход может быть одноразовым генератором. size должен быть положительным целым числом (bool запрещён), иначе ValueError.

Python 3.11, стандартная библиотека.
