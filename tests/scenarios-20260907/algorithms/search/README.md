# Левая граница поиска

Реализуйте lower_bound(values, target) для отсортированного списка: индекс первого элемента >= target, либо длина списка. Требуется O(log n), без копирования или сортировки. Обработайте пустой список и дубликаты.

Python 3.11, стандартная библиотека.
