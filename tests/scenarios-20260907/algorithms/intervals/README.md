# Объединение интервалов

Реализуйте merge(intervals): объединяйте пересекающиеся и касающиеся закрытые интервалы; результат отсортирован. Не меняйте вход. Интервал start > end вызывает ValueError. Пустой список даёт [].

Python 3.11, стандартная библиотека.
